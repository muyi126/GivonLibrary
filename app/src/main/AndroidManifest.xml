<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.base.givon.givonlibrary">

    <application android:allowBackup="true"
                 android:label="@string/app_name"
                 android:icon="@mipmap/ic_launcher"
                 android:supportsRtl="true"
                 android:name=".common.App"
                 android:theme="@style/AppTheme">
        <activity
                android:name=".ui.view.MainActivity"
                android:label="@string/app_name"
                android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.view.ActLoginActivity" android:screenOrientation="portrait"/>
        <!--订单列表-->
        <activity android:name=".ui.orderList.ActOrderLsitActivity"
                  android:screenOrientation="portrait"
                  android:parentActivityName=".ui.view.MainActivity"/>
    </application>

</manifest>
